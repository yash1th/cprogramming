#include<stdio.h>
int main(void)
{
  typedef int (*fnPtr) (void);
  return 0;
}
//Define a type FunctionPtr (using typedef)
//that represents a pointer to a function that
//returns an int and that takes no arguments.
//Refer to Chapter 11, “Pointers,” for the details on
//how to declare a variable of this type.
